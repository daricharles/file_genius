import 'package:flutter/material.dart';

const kBrand = Color(0xFF4A789C);
const kHover = Color(0xFFE8ECF8);
const double kSidebarW = 260.0; // or whatever width you want

// Add these color constants if they don't exist:
const Color kTextPrimary = Color(0xFF2D3748);
const Color kTextSecondary = Color(0xFF718096);

// AI model configuration
const String kDefaultGeminiModel = 'gemini-1.5-pro-latest';
const String kFallbackGeminiModel = 'gemini-1.5-flash-latest';
